# Frontend SDE-1 Interview Questions 

## React & JavaScript

1. Implement React Context.
2. Create a data structure for COVID cases by state and sort states in reverse alphabetical order.
3. From the same data, find the second highest case count.
4. Find values of `x` for which `(x² - x) < 0`.
5. Measure exactly 4L using a 5L jug and a 3L jug.
6. How would you deliver a project with best quality under constraints?
7. Explain `call`, `apply`, and `bind`.
8. Convert function parameters into an array.
9. Write an Array prototype method `duplicate()`.
10. Write a polyfill for `Array.map()`.
11. Write a polyfill for `Array.filter()`.

---

## DOM, Events & JavaScript

12. Implement jQuery chaining using plain JavaScript.
13. Print 100 buttons and alert the clicked button number using event bubbling.
14. Remove duplicates from an array using ES6.
15. Prevent multiple actions on the same event.
16. Handle maximum call stack size exceeded error.

---

## Objects, Closures & Functional JS

17. Read a nested object value using a string path.
18. Implement `sum(2)(3)` using closures.
19. List common HTTP response codes.
20. When to use PUT vs POST in REST APIs.
21. Output of `3 + 2 + "7"`.

---

## JavaScript Output-Based Questions

22. Variable scope and hoisting output.
23. `const` block scope behavior.
24. Temporal Dead Zone example.
25. Loose vs strict equality comparison.
26. `reduce()` output with initial value.
27. Working with `Set` in JavaScript.

---

## React

28. Write a functional component with state.
29. What is a pure component?
30. Why are keys used in React?
31. Explain Error Boundaries.
32. Explain `useRef` with example.
33. Why is `useMemo` used?
34. Difference between Redux and Context API.
35. Explain React lifecycle methods.
36. Explain Virtual DOM.

---

## CSS & HTML

37. CSS positions.
38. Pseudo-classes vs pseudo-elements.
39. CSS specificity output question.
40. Throttling vs debouncing.
41. Center a div using different CSS techniques.
42. Explain box model.
43. Inline vs inline-block.
44. Semantic HTML tags.
45. Media query for 1024px and 768px.
46. Create a circle using CSS.
47. Draw concentric circles using CSS.
48. Explain Flexbox auto margins.

---

## Browser, Performance & Networking

49. Explain hoisting in JavaScript.
50. How to reduce page load time.
51. Explain async/await.
52. How does `setTimeout` work with event loop?
53. Explain critical rendering path.
54. Local storage vs session storage.
55. Can passwords be stored in localStorage?
56. What happens when you type google.com in browser?
57. Why CORS exists and client-side error behavior.

---

## Advanced JavaScript

58. Prototype inheritance.
59. Object creation methods in JS.
60. Difference between `async` and `defer`.
61. Flatten a nested object.
62. Decode string: `a2c4e2 → aaccccee`.
63. Generator function to print prime numbers.
64. Polyfill for `Promise.all`.
65. Polyfill for `bind`.
66. Polyfill for `reduce`.

---

## System Design / UI

67. Implement a Kanban board with task movement rules.
68. Design a nested comment UI.
69. Create a file explorer in React using JSON.
70. Implement a responsive 3x4 fluid layout with fixed sidebar.
71. Move zeros to the left of an array.
72. Wiggle Sort I.
73. Max length subarray with target sum.

---
## JavaScript Outputs & Tricky Scenarios (Continued)

74. Output of deleting a variable inside an IIFE.
75. Behavior of `Object.create()` and prototype property deletion.
76. Output of `false == '0'` and `true === '1'`.
77. Output of reversing and pushing arrays (`john`, `jones` example).
78. Behavior of `var`, `let`, and `const` across files.
79. What happens when accessing a missing key in an object array?
80. Result of `reduce()` with an initial value.
81. Order of execution with multiple `setTimeout()` calls.

---

## Functions, Closures & Currying

82. Application of closures in real-world scenarios.
83. Write a function to access nested keys using multiple arguments.
84. Implement currying for variable number of inputs.
85. Write a function using closures to store state.
86. Explain why closures can cause memory leaks.
87. Generator function to print all prime numbers.
88. Difference between closures and scope.

---

## Objects & Data Transformation

89. Flatten a nested object into a POJO with dot notation keys.
90. Populate a template object using a data object.
91. Remove a specific key from objects without mutating original object.
92. Find Min and Max from an array.
93. Decode URL search strings in JavaScript.
94. Which JS function decodes URL-encoded strings?
95. Flatten an object using recursion.

---

## Arrays & Algorithms

96. Move all zeros to the left of an array.
97. Wiggle Sort I.
98. Max length subarray with given target value.
99. Split string into equal chunks based on input size.
100. Remove duplicates from an array using ES6.
101. Find second highest value in an array.
102. Print numbers in reverse order using `setTimeout`.

---

## React Advanced

103. Why are keys important in React lists?
104. Can `useRef` be used as state? Why or why not?
105. Difference between Redux and Context API.
106. When should you use Redux over Context?
107. How do you optimize performance in React?
108. What is Error Boundary in React?
109. Explain React Hooks.
110. Explain `useRef` with example.
111. Explain `useMemo` and `useCallback`.
112. What is a Pure Component?
113. Explain React lifecycle methods.

---

## CSS Advanced & Layouts

114. CSS Box Model explanation.
115. Difference between inline, inline-block, and block.
116. Explain Flexbox auto margin behavior.
117. Create a responsive 3×4 fluid layout with fixed sidebar.
118. Media query for devices 1024px and 768px.
119. Draw concentric circles using CSS.
120. Create a circle using HTML & CSS.
121. Explain CSS specificity using example.
122. Pseudo-classes vs pseudo-elements.
123. Explain `position` values in CSS.
124. How to center a div (multiple approaches).

---

## Browser, Performance & Networking

125. Explain Critical Rendering Path.
126. How to reduce page load time in JavaScript.
127. Explain async/await.
128. Explain Event Loop.
129. How does `setTimeout` work internally?
130. What happens when you type a URL in the browser?
131. Why CORS exists?
132. Why does CORS error occur on client side?
133. JSON.parse vs JSON.stringify.
134. JWT Tokens and their usage.
135. Can passwords be stored in localStorage?

---

## HTML Fundamentals

136. Semantic HTML tags.
137. Difference between HTML and HTML5.
138. What does `<!DOCTYPE>` do?
139. Difference between inline and inline-block elements.
140. What is a `<span>` tag?
141. How to define `list-style-type` in CSS?
142. What happens when only `::before` is defined without content?

---

## Polyfills & Advanced JS

143. Polyfill for `Promise.all`.
144. Polyfill for `Array.reduce`.
145. Polyfill for `Function.bind`.
146. Custom implementation of `map`, `filter`.
147. Write a duplicate function using prototype.
148. Explain prototype inheritance.

---

## System Design / UI Problems

149. Design a Kanban board with task movement rules.
150. Design a nested comment UI.
151. Create a file explorer in React using JSON.
152. Design scrollable, responsive layout with fixed sidebar.

---
